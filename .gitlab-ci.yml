image: node:14.15.4

pages:
  artifacts:
    paths:
      - build
  cache:
    paths:
      - node_modules/.cache
  only:
    - main
  script:
    - yarn
    - yarn build
    - rm -rf public
    - cp build/index.html build/404.html
    - mv build public
  stage: deploy
